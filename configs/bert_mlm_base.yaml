defaults:
  - base

model:
  type: "bert"
  d_model: 512
  n_heads: 8
  n_layers: 6
  dim_feedforward_mult: 4

training:
  checkpoint_dir: "./checkpoints/bert_mlm"

mlm:
  masking_ratio: 0.20
  
components:
  use_rope: true
  use_flash_attention: true
  compile_model: false