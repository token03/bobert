data:
  db_path: './data/beatmaps.db'
  max_seq_len: 1023
  in_channels: 10
  val_split: 0.1
  chunk_size: 1000
    
training:
  batch_size: 8
  num_epochs: 5
  learning_rate: 5e-4
  weight_decay: 0.05
  warmup_ratio: 0.05
  min_lr: 1e-6
  use_amp: true
  checkpoint_dir: "./checkpoints"
  grad_clip_norm: 1.0
  
  # Sampling configuration
  sampling:
    method: "kde"  # "kde", "temperature", or "weighted"
    kde_bandwidth: 0.3  # Bandwidth for KDE sampling
    temperature: 2.0    # Temperature for temperature sampling
    difficulty_index: 3  # Index of difficulty rating in metadata
  
model:
  dropout: 0.1
  
components:
  use_rope: true
  use_flash_attention: true
  compile_model: false
  
mlm:
  masking_ratio: 0.20